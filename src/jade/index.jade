extends layout

block css
  link(rel="stylesheet" href='bin/css/index.css')

block js
  script(src="bin/js/index.js")

block content
  .viewport
    .overlay
      .caption-area
    .underlay
      .clockwall
        - numClocksWide = 15
        - numClocksTall = 9
        - for(var i = 0; i < numClocksWide * numClocksTall; ++i)
          .clock
            .face
            .hand
            .hand
            - if (i == Math.floor(numClocksWide * numClocksTall / 2))
              .hand.second
              .axle